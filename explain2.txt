Wait, does Antonia's sheet have an `ESTATUS` column?
Yes, `DEFAULT_SALES_HEADERS = ['FOLIO', 'CLIENTE', 'CONCEPTO', 'VENDEDOR', 'FECHA', 'F. ENTREGA', 'ESTATUS', 'COMENTARIOS', 'ARCHIVO', 'MONTO', 'F2', 'COTIZACION', 'TIMELINE', 'LAYOUT', 'AVANCE']`

Wait, when the task is assigned, is there a chance that `ESTATUS` or `AVANCE` was mistakenly updated in `apiSaveTrackerBatch` or `apiUpdateTask`?
Look at `apiSaveTrackerBatch`. It calls `internalBatchUpdateTasks(personName, ...)` directly.
If she creates a task on the frontend, `ESTATUS` is empty.
In `internalBatchUpdateTasks`, if it's a NEW row:
```javascript
              const statusIdx = getColIdx('ESTATUS');
              if(statusIdx > -1 && !newRow[statusIdx]) newRow[statusIdx] = 'ASIGNADO';
```
So `ESTATUS` becomes `"ASIGNADO"`. "ASIGNADO" is not in `doneStatuses`.
What about `AVANCE`?
If it's empty, `newRow[avanceIdx]` is `""`.

So when she saves, the row is inserted. `isComplete` is false.
Then, wait. What if she changes the `VENDEDOR` using the frontend selector, and then saves?
Then `taskData` has `ESTATUS = "ASIGNADO"` and `AVANCE = ""`.

Wait! Look at `index.html`!
```javascript
            const isCompleted = Object.entries(row).some(([k, v]) => (String(k).toUpperCase().includes('AVANCE') && String(v) === '100') || (String(k).toUpperCase().includes('CUMPLIMIENTO') && String(v).toUpperCase() === 'SI'));
```
The frontend has some logic to check if completed.
But wait. What if the bug is NOT in `internalBatchUpdateTasks`?
What if the bug is that the Vendor saves, and the Vendor's save triggers Reverse Sync, which INCORRECTLY marks it as complete for Antonia?

Let's read `Reverse Sync` in `apiUpdateTask`:
When Vendor (e.g. `ANGEL SALINAS (VENTAS)`) saves a task, `isAntonia` is false.
`distributionTasks` has the task he saved.
```javascript
          // Handle Reverse Sync (Vendor -> Antonia)
          if (!isAntonia && distributionTasks.length > 0) {
               const syncPayloads = [];
               let antDataFetched = false;
               let antDataRows = [];

               distributionTasks.forEach(taskData => {
                   const getTVal = (keys) => { ... };
                   const estatus = String(getTVal(['ESTATUS', 'STATUS', 'ESTADO'])).toUpperCase().trim();
                   const avanceRaw = String(getTVal(['AVANCE', 'AVANCE %', '% AVANCE', '%', 'CUMPLIMIENTO'])).replace(/%/g, '').trim();
                   const avanceNum = parseFloat(avanceRaw);
                   const isDone = estatus === 'HECHO' || estatus === 'TERMINADO' || estatus === 'FINALIZADO' || estatus === 'REALIZADO' || estatus === 'COMPLETADO' || estatus === 'DONE' || avanceRaw === '100' || avanceNum === 100 || avanceRaw.toUpperCase() === 'SI';
                   const tFolio = String(getTVal(['FOLIO', 'ID'])).toUpperCase().trim();

                   if (tFolio && isDone) {
```
If the Vendor's task is `isDone`, it updates Antonia's log to `DONE`.

BUT WAIT! What if the task is NOT done?
```javascript
               // Sincronización Reversa hacia Antonia (NO está completa si no termina en VENTAS ni está DONE)
               const antoniaReverseSyncTasks = [];
               distributionTasks.forEach(t => {
                   let existingTaskFolioRev = null;
                   Object.keys(t).forEach(k => {
                       if ((k.toUpperCase().trim() === 'FOLIO' || k.toUpperCase().trim() === 'ID') && t[k]) {
                           existingTaskFolioRev = t[k];
                       }
                   });
                   const reverseFolio = existingTaskFolioRev || t['FOLIO'] || t['ID'];
                   if (reverseFolio && String(reverseFolio).toUpperCase().startsWith("AV-")) {
                       antoniaReverseSyncTasks.push(t);
                   }
               });
               if (antoniaReverseSyncTasks.length > 0) {
                   const safeRevTasks = antoniaReverseSyncTasks.map(t => {
                       let st = Object.assign({}, t);
                       const delKeys = ['ESTATUS', 'STATUS', 'ESTADO', 'AVANCE', 'AVANCE %', '% AVANCE', '%', 'CUMPLIMIENTO'];
                       Object.keys(st).forEach(k => {
                           if (delKeys.includes(k.toUpperCase().trim())) delete st[k];
                       });
                       const matchedSync = syncPayloads.find(sp => sp.FOLIO === st.FOLIO || sp.ID === st.FOLIO || sp.FOLIO === st.ID);
                       if (matchedSync) {
                           st['MAP COT'] = matchedSync['MAP COT'];
                           st['PROCESO_LOG'] = matchedSync['PROCESO_LOG'];
                       }
                       return st;
                   });
                   internalBatchUpdateTasks("ANTONIA_VENTAS", safeRevTasks, false);
               }
```
If the Vendor's task is NOT done, it pushes the task to `antoniaReverseSyncTasks`.
Then it removes `ESTATUS`, `STATUS`, `ESTADO`, `AVANCE`, `CUMPLIMIENTO` from the payload!
And it updates `ANTONIA_VENTAS` with `safeRevTasks`.
Wait, `internalBatchUpdateTasks` receives this payload.
When `internalBatchUpdateTasks` processes an update, it ONLY updates the columns present in the payload!
So it leaves `ESTATUS` and `AVANCE` untouched in Antonia's sheet.
Is this what completes it? No, if it's untouched, and it wasn't complete before, it's still not complete.

Wait! What if the Vendor updates the task, and the vendor's task HAS A DIFFERENT ESTATUS?
Wait, if `internalBatchUpdateTasks("ANTONIA_VENTAS", safeRevTasks, false)` is called, does it auto-archive?
`internalBatchUpdateTasks` ALWAYS runs the auto-archive logic at the end!
```javascript
        activeRows.forEach(row => {
            let isComplete = false;
            const valEstatus = estatusIdx > -1 ? String(row[estatusIdx] || "").toUpperCase().trim() : "";
```
Since it runs over ALL `activeRows`, if any row was magically complete, it archives it. But why would it be complete NOW if it wasn't before?

Let's go back to the exact bug description:
"En su tabla de Ventas cuando asigna una actividad, por alguna razón se pasa a tareas realizadas sin tener el 100%, sin embargo al vendedor que se le asignó si permanece en su tabla"
This implies Antonia is in HER Sales table. She assigns the activity (e.g. changes `VENDEDOR` column).
She hits Save. The row IMMEDIATELY goes to Tareas Realizadas!
But the vendor gets the row and it stays active.
